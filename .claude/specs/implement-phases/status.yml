slug: implement-phases
origin: spec
complexity: complex
status: VERIFIED_WITH_CONDITIONS
created: 2026-09-08
updated: 2026-09-08
history:
  - { at: 2026-09-09T09:30Z, from: IN_PROGRESS, to: VERIFIED_WITH_CONDITIONS, actor: human,
      note: "Accepted. Twelve assertions with five statements reverted one at a time and
             each watched failing. The five scope conditions carry forward." }
  - { at: 2026-09-09T03:20Z, from: READY_WITH_CONDITIONS, to: IN_PROGRESS, actor: agent,
      note: "T1-T7 implemented. The stage is five phases and says so — it was seven with
             eight stops, and a --human unit that changes how often it interrupts a
             person may not do that silently. Fetch and plan became reads; the prompt
             file is the whole keep-set, as the audit required. Three flags deleted with
             per-flag reasons, --fix added, and the two lists now provably agree. An
             external-action sub-gate was removed: it contradicted the Handoff section
             two screens above it, which has always said this stage never delivers.
             Twelve assertions added; five statements reverted one at a time and each
             watched failing. Two reverts silently did not apply on the first attempt
             because the sentences wrap mid-phrase — the same trap flat() exists for,
             met from the other side." }
  - { at: 2026-09-09T01:10Z, from: NEEDS_REVISION, to: READY_WITH_CONDITIONS, actor: human,
      note: "Gate passed, no second audit iteration. Each finding is closed by a named
             change, and the trail is here rather than implied by the advance:
             blocker (both families) 'AC8 leaves criteria unasserted' → every criterion
             now marked guarantee or description, AC8 covers all six guarantees;
             blocker (both) 'keep-whatever is unbounded' → keep-set closed to the prompt
             file; major (grok) 'T1 defers three flags, and keeping --complexity
             contradicts AC4' → all three deleted in the spec with per-flag reasons;
             major (gpt) 'AC6/T5 conflict on null-tracker durability' → resolved against
             AC6's wording, the artifacts are transient and not committed; major (both)
             'AC7 undefined' → mapping made mechanical against the task-line AC: suffix;
             minor (grok) 'AC5 negative-only assertion' → AC5 re-promoted and asserted
             positively; minor (gpt) 'T6 does not say where watched-failure evidence
             lives' → the commit message, which is where Article 3 sends an auditor.
             The revision answering the last major landed AFTER the rebuttals, so the
             escalate verdict is real in protocol and stale in substance. Recorded as
             conditions rather than as a clean READY." }
  - { at: 2026-09-09T00:40Z, from: SCOPE_AUDIT, to: NEEDS_REVISION, actor: agent,
      note: "escalate. Two blockers, raised independently by both responding families:
             the spec cited Article 3 while leaving four of its own criteria as
             guarantees with no assertion, and T3's keep-whatever clause was unbounded
             (unique work cannot name a supplement). Both taken — every criterion is
             now marked guarantee or description, and the keep-set is closed to one
             item. The three deferred flag decisions were also decided here rather
             than in implementation; one critic caught that keeping --complexity
             contradicts AC4. The revision answering the remaining major landed after
             the rebuttals, so the escalation is stale in substance and real in
             protocol. Implementation is NOT taken: run_mode is human, and a --human
             unit stops at every gate. The decision is whether the revised spec clears
             the gate or needs a second audit iteration." }
  - { at: 2026-09-09T00:20Z, from: SCOPE_AUDIT, to: SCOPE_AUDIT, actor: agent,
      note: "panel narrowed to two families. One seat recorded dispatch_failure three
             times on a 189-line brief, timing out at 600s; the engine labelled it
             rather than counting it as a vote. Cause measured: no timeout binary on
             PATH, so the shell-level cap was not applied — the engine warned about
             exactly this and coreutils was installed, after which the cap applied and
             the seat still timed out. Key retired with a reason so the debate could
             conclude; the verdict must be read as two families, not three." }
  - { at: 2026-09-08T23:05Z, from: DRAFTING_TASKS, to: SCOPE_AUDIT, actor: agent,
      note: "scope audit dispatched, panel cpd, three families. Brief carries nine
             contradictions between the document and the pipeline, and asks six
             questions the spec cannot answer about itself — including whether
             scoping the Article 2 fix to one file is allowed while 41 tracked files
             keep the product name. None is under governance/, so Article 1 is clean;
             the article-2 question is whether a driver's name leaking into the
             abstract layer is the same violation as naming a product outright." }
  - { at: 2026-09-08T23:00Z, from: PRD_READY, to: DRAFTING_TASKS, actor: agent,
      note: "requirements and tasks written; complexity complex was set at triage
             and is read from here, not re-derived" }
  - { at: 2026-09-08T10:00Z, from: null, to: TRIAGE, actor: human, note: "created — dogfood run: the pipeline builds its own missing pieces" }
  - { at: 2026-09-08T11:00Z, from: DRAFT_PRD, to: PRD_READY, actor: human, note: "PRD approved" }
  - { at: 2026-09-08T10:05Z, from: TRIAGE, to: DRAFT_PRD, actor: agent, note: "triage: spec path (roles.tracker null, ticket path unavailable); complexity complex" }
in_flight: false
audit_iterations: 1
last_audit: { step: scope, verdict: escalate, iter: 1, proposal_ref: u5-scope,
              governance_version: null, panel: cpd,
              panel_reason: "two families, not three — the third seat timed out on this brief three times and was retired with a recorded reason",
              evidence_digest: ".claude/runs/implement-phases/u5-scope" }
last_impl: { commit: null }
depends_on: []
superseded_by: null
accepted_conditions:
  - "AC3 and AC5 were demoted from guarantee to description where no script can
     produce their failing case; AC5 was then re-promoted with a positive
     assertion after a critic showed a negative-only check passes a file where
     the flag was deleted. Any later claim that AC3 is asserted is false."
  - "The keep-set from the removed phases is closed to exactly one item, the
     prompt file. Anything else found to be unique during implementation is a
     NEW finding against this unit, not an addition to the set."
  - "check.py's transit-artefact rule is the reason the two artifacts are not
     committed, NOT the assertion for AC6 — it does not name them. AC6 needs its
     own assertion."
  - "The panel ran with two families, not three. The verdict is to be read as
     narrower than configured."
  - "Article 2 stays open. This unit clears one file; 41 tracked files keep the
     product name. Completing this unit is not evidence the article is cleared."

run_mode: human
