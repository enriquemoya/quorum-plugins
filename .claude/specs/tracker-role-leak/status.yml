slug: tracker-role-leak
origin: spec
complexity: complex
status: VERIFIED_WITH_CONDITIONS
created: 2026-09-09
updated: 2026-09-09
history:
  - { at: 2026-09-09T09:30Z, from: IN_PROGRESS, to: VERIFIED_WITH_CONDITIONS, actor: human,
      note: "Accepted. Two of three critics passed at round 2; the third's remaining major
             is recorded as a condition rather than argued away — co-occurrence is a
             lexical invariant and an inert reference satisfies it." }
  - { at: 2026-09-09T09:00Z, from: SCOPE_AUDIT, to: IN_PROGRESS, actor: agent,
      note: "Swept. 239 mentions to zero outside the driver and the two abstraction files.
             The audit changed three things before an edit was made. It caught the spec
             citing Article 2 for a rule the constitution does not contain — there is no
             layer clause, and 'the driver may name it, the agnostic layer may not' is a
             design rule that had been given borrowed authority. It caught the gate
             bounding ~45 of 239 occurrences while the spec called the split honest;
             co-occurrence, proposed by a critic, made the rest checkable and failed on
             27 of 35 files at the time it was written. And it caught 23 of 35 files
             having no owning task.
             Implementation found a fourth thing the audit had asked about: the vault
             tree legitimately LINKS to a tracker without needing one, so deleting the
             mention would have lost something real. Three operations, not two —
             delegate, genericise, delete.
             Residue the first sweep missed because the verification was case-sensitive:
             a named MCP server in four agents' tools lists, a `jira:` frontmatter field
             in the note schema, and the driver invoked by skill id rather than by role.
             Evidence: three injections caught, the driver passes." }
  - { at: 2026-09-09T06:40Z, from: TRIAGE, to: DRAFT_PRD, actor: agent, note: "triage: spec path; complexity complex — 35 files, and the fix is structural rather than a rename" }
  - { at: 2026-09-09T06:35Z, from: null, to: TRIAGE, actor: agent,
      note: "created from a scope-audit finding on implement-phases, which fixed one file
             and recorded that the article stayed open. Measured since: 203 occurrences in
             35 agnostic files, concentrated in the three closest to the ticket path. Not
             illustrative examples — direct calls to a named tracker's tools, which is the
             shape Article 1 does NOT permit. Nothing under governance/, so Article 1 is
             clean; the driver skill keeps its name because a driver naming what it drives
             is what the article allows." }
in_flight: false
audit_iterations: 1
last_audit: { step: scope, verdict: escalate, iter: 1, proposal_ref: u9-scope,
              governance_version: null, panel: cpd, panel_reason: "three families",
              evidence_digest: ".claude/runs/tracker-role-leak/u9-scope" }
last_impl: { commit: null }
depends_on: []
superseded_by: null
accepted_conditions:
  - "The check is a LEXICAL invariant. It proves a file naming a tracker also
     names an approved way to reach one; an inert reference satisfies it, and it
     proves no delegation correct. One critic held this as a major and did not
     withdraw."
  - "Nineteen files had tracker mentions deleted, changing conventions that no
     suite exercises — the e2e count says nothing about the tracker path. Over-
     deletion is additive and cheap to restore, and is recorded as a known
     non-goal rather than left to be discovered as a regression."
  - "The classification question — does this file read or write tracker state —
     was answered once per file and written into an inventory. Nothing re-checks
     that the answers were right; the inventory is a record, not a gate."

run_mode: agent
