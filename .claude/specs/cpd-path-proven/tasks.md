# Tasks — the cross-provider path, run and corrected

## Phase 1 — correct the skill against what ran

- [x] T1: record the observed refusal — files: plugins/quorum-orchestrator/skills/quorum-panel/SKILL.md — AC: AC1
      Replace the property with the measurement: round 2 absent, exit 2, empty
      stdout, missing pairings on stderr. Re-taken on the current engine rather
      than inherited. Names the pipe trap that has swallowed it in practice.

- [x] T2: the contract is appended AND embedded — files: plugins/quorum-orchestrator/skills/quorum-panel/SKILL.md — AC: AC2
      cpd-run dispatches with the severity floor set, so the layer appends it;
      embedding it in the brief as well went with 2-of-3 to 3-of-3 on one
      panel. Stated as one observation, not a rate. Warns against deduplicating
      the two copies.

- [x] T3: forbid fencing the terminal block — files: plugins/quorum-orchestrator/skills/quorum-panel/SKILL.md — AC: AC3
      A fenced block reads as a quoted example: findings discarded, verdict
      still counted, so the seat votes on nothing.

## Phase 2 — the evidence

- [x] T4: commit the transcripts, anchored — files: .claude/runs/cpd-path-proven/ — AC: AC4
      Three runs with an engine-state column; the two probes and the parser
      debate deliberately left out, with the reason recorded.

## Phase 3 — the gate

- [ ] T5: run the suites — files: scripts/check.py, scripts/e2e.py — AC: AC1, AC2, AC3, AC4
